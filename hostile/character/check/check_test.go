package check

import (
	"testing"
	// "github.com/Galdoba/TravellerTools/hostile/character/career"
)

func TestParseCode(t *testing.T) {
	// for _, cr := range career.LoadAllDEBUG() {
	// 	statCode, tn, err := ParseCode(cr.Qualification)
	// 	if err != nil {
	// 		t.Errorf("err qualif: %v (%v %v) = %v", cr.Name, statCode, tn, err.Error())
	// 	}

	// 	statCode, tn, err = ParseCode(cr.QualificationReq)
	// 	if err != nil {
	// 		t.Errorf("err qualifreq: %v (%v %v) = %v", cr.Name, statCode, tn, err.Error())
	// 	}

	// 	statCode, tn, err = ParseCode(cr.Commision)
	// 	if err != nil {
	// 		t.Errorf("err commision: %v (%v %v) = %v", cr.Name, statCode, tn, err.Error())
	// 	}

	// 	statCode, tn, err = ParseCode(cr.Advance)
	// 	if err != nil {
	// 		t.Errorf("err advance: %v (%v %v) = %v", cr.Name, statCode, tn, err.Error())
	// 	}

	// 	statCode, tn, err = ParseCode(cr.Survival)
	// 	if err != nil {
	// 		t.Errorf("err survivel: %v (%v %v) = %v", cr.Name, statCode, tn, err.Error())
	// 	}
	// }
}
